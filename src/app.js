let loggedin = 'false'

function login() {
    if (loggedin == 'false') {
        // Login with API and user invite

        var passkey = document.getElementById('passkey-box').value

        console.log(passkey)
    } else {
        // Send saved logins
    }
}